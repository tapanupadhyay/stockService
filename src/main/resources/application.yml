spring :
  application:
    name = stockService
server :
  port : 8301



#eureka:
 # client:
  #  register-with-eureka: true
   # fetch-registry: true
    #service-url:
    #  defaultZone: http://localhost:8070/eureka/
    #instance:
     # hostname : localhost